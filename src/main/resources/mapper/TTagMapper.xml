<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiami.dao.TTagMapper">
    <resultMap id="BaseResultMap" type="com.xiami.entity.TTag">
        <!--@mbg.generated generated on Thu Jul 30 12:38:59 CST 2020.-->
        <!--@Table t_tag-->
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>
    <sql id="Base_Column_List">
        <!--@mbg.generated generated on Thu Jul 30 12:38:59 CST 2020.-->
        id, `name`,create_time, update_time
    </sql>

    <select id="selectBySearch" parameterType="com.xiami.dto.TagQueryDto" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_tag
        <where>
            <if test="tagName != null and tagName != ''">
                AND name=#{tagName}
            </if>
            <if test="beginTime != null and beginTime != ''">
                AND DATE_FORMAT(t.create_time, '%Y-%m-%d' ) &gt;= #{beginTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND DATE_FORMAT(t.create_time, '%Y-%m-%d' ) &lt;= #{endTime}
            </if>
        </where>
    </select>

    <delete id="deleteByIds">
        delete from t_tag
        where id in
        <foreach collection="array" open="(" close=")" separator="," item="id">
            #{id}
        </foreach>
    </delete>
</mapper>